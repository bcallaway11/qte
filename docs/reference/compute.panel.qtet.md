# compute.panel.qtet

`compute.panel.qtet` uses third period of data, combined with
Distributional Difference in Differences assumption (Fan and Yu, 2012)
to point identify QTET.

## Usage

``` r
compute.panel.qtet(qp)
```

## Arguments

- qp:

  QTEparams object

## Value

QTE object
